# 2026-07-07 成语绘本工坊 - 提交推送

## 操作
- 将工作区代码改动提交到 `main` 并推送到 `origin/main`（commit `0fd6ec0`，连同之前未推送的 `eab551a`）。
- 提交说明：`feat(security): 图片下载域名白名单校验与路径安全加固`。
- 改动内容：lib/security.ts 路径/URL 安全校验、lib/save-book.ts 下载域名校验（防 SSRF）、.env.example/README 新增 ALLOWED_IMAGE_DOMAINS、多个组件与首页小幅调整。

## 用户决策（重要约定）
- 提交目标：直接提交到 `main` 并推送（未使用 feature/multi-category-expansion 分支）。
- 图片策略：**只提交源码，不提交 `public/generated` 下的生成图片**（约 531MB 的 PNG 与 index.json 均保留在本地、未纳入版本库）。
- 注意：`feature/multi-category-expansion` 分支已跟踪生成图片（269 张），与 main 当前策略不一致，后续如需统一需明确。

## 补充：将 public/generated 加入 .gitignore（commit `424ca52`）
- .gitignore 新增 `public/generated/` 忽略规则（可再生成资源，不纳入版本库）。
- 用 `git rm --cached -r public/generated` 停止跟踪已提交的 269 个生成文件（本地磁盘 396 个文件保留，仅解除版本控制）。
- 已推送到 origin/main：`0fd6ec0..424ca52 main -> main`。
- 后续影响：他人 pull main 后本地 `public/generated` 会被移除（需重新生成）；feature 分支若合并到 main 会与忽略策略冲突，需提前处理。

## 补充：删除 feature/multi-category-expansion 分支
- 该分支有 58 个未合并到 main 的提交（task worker、多分类扩展等），但**从未推送到 origin**（仅本地分支）。
- 用户确认「本地+远程都删除」；本地 `git branch -D` 成功，远程 `git push --delete` 因 ref 不存在而 no-op。
- 删除前已在本地打安全 tag `archive/feature-multi-category-expansion`（指向 114128b，未推送），可随时恢复：
  `git branch feature-multi-category-expansion archive/feature-multi-category-expansion`
- 用户偏好：采用 main 直推工作流，功能分支用完即删。