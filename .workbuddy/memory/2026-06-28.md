# 2026-06-28 成语绘本工坊 - 批量 Bug 修复

## 修复批次
通过 SoftwareCompany 团队（engineer + QA），完成 11 项代码修复，涉及 14 个文件。

## 修复内容
- **安全**：移除 batch-generate.js 硬编码 API Key，创建 .env.example
- **ESLint**：降级 eslint-config-next 至 14.x，新建 .eslintrc.json
- **内存泄漏**：BookViewer.tsx/BookCard.tsx 使用 useMemo 缓存 Blob URL + useEffect cleanup
- **并发**：TaskExecutor 添加 AbortController、isRunning 检查、循环替代递归
- **页面优化**：generate/page.tsx 自动跳转 /library；page.tsx useRef 管理 executor + useCallback
- **字体**：layout.tsx 集成 next/font Noto_Sans_SC，globals.css 移除 @import
- **类型安全**：types.ts 新增 PreGeneratedIndexItem/SceneTemplateRaw/DecompositionRaw，4 个文件移除 any
- **性能**：task-store.ts persistDebounced 300ms 防抖
- **构建**：tsc --noEmit 零错误，eslint 零错误（仅 4 个 img 标签 warning 保留）

## 第二轮修复（25 个 Bug 深度修复）
通过代码审查发现 25 个 Bug（🔴5 + 🟡8 + 🟢12），SoftwareCompany 团队修复 + QA 验证全部通过。

### 🔴 严重
- db.ts: Scene 表改用 ++id 自增主键 + 复合索引 [bookId+sceneId]
- store.ts: saveCurrentBook 空值守卫
- generate.ts: 视频 URL→videoId 修正；数组索引可选链 data?.[0]?.url
- pdf.ts: 图片加载 onerror reject + 10s 超时

### 🟡 中等
- BookViewer.tsx: 朗读逻辑改 useTTS speakScenes()
- task-executor.ts: isRunning 设 true 提前消除竞态窗口
- generate.ts: Server Action 重试 5→2 次
- db.ts: saveRecommendedIdioms 加事务保护
- generate/page.tsx: cleanup 重置 jobCreatedRef；新任务重置 failed
- decompose.ts: JSON.parse try-catch 含原始片段
- page.tsx: loadPersistedTasks 后自动启动 pending job；handleBatchGenerate 防重点击

### 🟢 轻微
- agnes-api.ts: size 参数实际使用
- SceneCard.tsx: clipboard API 优先
- TaskCard.tsx: useShallow 合并 selector
- BookCard.tsx: 依赖 book.scenes
- read/[id]: fetch 加 AbortController
- IdiomSelector: fetch 加 AbortController
- recommend.ts: exclude 上限 50
- 3 项接受现状跳过
